package net.developia.mapper;

import org.apache.ibatis.annotations.Select;

public interface TimeMapper {
	
	// 구현체를 안만들게 해놓음 ...  MyBatis-Spring
	@Select("SELECT sysdate FROM dual")
	public String getTString();
}
